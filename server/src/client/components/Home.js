import React from "react";

const Home = () => {
  return (
    <div>
      <div>HOME</div>
      <button onClick={() => console.log("pressed")}>Press me</button>
    </div>
  );
};

export default Home;
